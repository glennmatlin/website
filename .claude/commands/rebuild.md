---
description: Build the Quarto website and show any errors or warnings
---

Rebuild the entire Quarto website and report any issues:

```bash
cd /Users/glenn/codespace/website && quarto render 2>&1
```

After the build completes:
1. Report any errors or warnings
2. Confirm the build succeeded
3. Note if any files failed to render
