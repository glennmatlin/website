---
description: Start the Quarto development server with live reload
---

Start the Quarto preview server in the background:

```bash
cd /Users/glenn/codespace/website && quarto preview --no-browser
```

The site will be available at http://localhost:4000

Note: The preview server will continue running in the background. Use Ctrl+C in the terminal or kill the process to stop it.
